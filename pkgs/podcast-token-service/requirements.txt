# For the Podman / Umbrel VPS path.
# On NixOS, dependencies are managed by the Nix derivation instead.
#
# Requires libsecp256k1 as a system package before installing:
#   Debian/Ubuntu: apt install libsecp256k1-dev
#   Alpine:        apk add secp256k1-dev
#
# The Podman image handles this automatically — you do not need to run
# pip manually. This file is here for reference and local development.
fastapi
uvicorn[standard]
aiosqlite
httpx
python-dotenv
cryptography
cffi
websockets
prometheus-client
